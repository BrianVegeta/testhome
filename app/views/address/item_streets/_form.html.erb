<%= form_for(@address_item_street) do |f| %>
  <% if @address_item_street.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@address_item_street.errors.count, "error") %> prohibited this address_item_street from being saved:</h2>

      <ul>
      <% @address_item_street.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
