= render 'address', {f: f}
= render 'col_pattern_suit', {f: f}
= render 'plain', {f: f}
= render 'floor', {f: f}
= render 'direction', {f: f}
= render 'device', {f: f}
= render 'furniture', {f: f}
= render 'parking', {f: f}
= render 'rent_deposit', {f: f}
= render 'rent_include', {f: f} 
= render 'management_fees', {f: f}
= render 'col_rent_period', {f: f}
= render 'col_allow_move_in', {f: f}
= render 'identity_require', {f: f}
= render 'col_sexual_require', {f: f}
= render 'cooking_allow', {f: f}
= render 'pet_allow', {f: f}
= render 'nearby', {f: f}
= render 'nearby_traffic', {f: f}
= render 'title', {f: f}
= render 'description_editor', {f: f}  
= render 'photo', {f: f}