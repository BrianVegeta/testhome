<h1>Listing sites_admin_items</h1>

<%= link_to 'New Item', new_sites_admin_item_path %>
<br>

<table class="table">
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sites_admin_items.each do |item| %>
      <tr>
        <td><%= item.id %></td>
        <td><%= item.name %></td>
        <td><%= item.type %></td>
        <td><%= link_to "查看", sites_admin_item_path(@organization.id, item.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>