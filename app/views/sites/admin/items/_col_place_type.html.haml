.form-group
  %label.control-label
    %span{style: 'color: red;'} *
    場地類型
  .col{class: "#{f.error_class :place_type}"}
    - data = { errormessage: "#{f.error_message :place_type}" }
    - types = Item::RentPlace::TYPES.map {|k, v| [v, k] }
    = f.select :place_type, types, {}, {class: 'form-control error-tooltip', data: data}
.form-group.group-lg-hr