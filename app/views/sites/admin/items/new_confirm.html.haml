= render 'step_bar', locals: {step: params[:step]}
.row
  .col-sm-12
    = render 'show_item'
= render 'form'

= link_to "prev step", new_sites_admin_item_path(step: :fill_form)
= link_to "confirm", '#'