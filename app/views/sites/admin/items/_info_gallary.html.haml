.item-container
  %h3.over-write 圖片
  - if photos.length > 0 
    .item-gallary
      - photos.each do |p|
        .item-photo
          %img{src: "#{p.file.url(:public)}"}
  - else
    .item-else 等待圖片上傳。
  
