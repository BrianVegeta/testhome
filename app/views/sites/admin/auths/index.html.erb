<h1>Listing sites_admin_auths</h1>

<table class="table">
  <thead>
    <tr>
      <th>ID</th>
      <th>type</th>
      <th></th>
      <th>來源組織</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @sites_admin_auths.each do |sites_admin_auth| %>
      <tr>
        <td><%= sites_admin_auth.id %></td>
        <td><%= sites_admin_auth.name %></td>
        <td><%= NewUser.find(sites_admin_auth.new_user_id).email %></td>
        <td><%= sites_admin_auth.organization_id %></td>
        <td>
          <%= link_to 'Show', '' %>
          <%= link_to 'Edit', '' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Auth', new_sites_admin_auth_path %>
