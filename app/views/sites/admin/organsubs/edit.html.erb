<% if @parent %>
  <h1>編輯子組織 <small>上層：<%= @parent.name %></small></h1>
<% else %>
  <h1>編輯組織</h1>
<% end %>

<%= render 'form' %>

<%= link_to '內容', sites_admin_organsub_path(@organization.id, @sites_admin_organsub.id) %> | 
<%= link_to '返回列表', sites_admin_organsubs_path %>
<% if @parent %>
 | <%= link_to '返回上層組織', sites_admin_organsub_path(@organization.id, @parent) %>
<% end %>