<%= form_for(@sites_organization_post_list) do |f| %>
  <% if @sites_organization_post_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sites_organization_post_list.errors.count, "error") %> prohibited this sites_organization_post_list from being saved:</h2>

      <ul>
      <% @sites_organization_post_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
